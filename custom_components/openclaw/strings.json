{
  "config": {
    "step": {
      "user": {
        "title": "Connect to OpenClaw Gateway",
        "description": "Configure connection to your OpenClaw Gateway",
        "data": {
          "host": "Host",
          "port": "Port",
          "token": "Gateway Token",
          "use_ssl": "Use SSL (wss://)",
          "timeout": "Agent Timeout (seconds)"
        }
      },
      "session": {
        "title": "Session and Speech",
        "description": "Choose a session and speech options",
        "data": {
          "session_key": "Session Key (default: main)",
          "model": "Model override (optional)",
          "thinking": "Thinking mode override (optional)",
          "strip_emojis": "Strip emojis from TTS speech",
          "tts_max_chars": "TTS max characters (0 = no limit)"
        }
      },
      "reauth_confirm": {
        "title": "Re-authenticate OpenClaw Gateway",
        "description": "The gateway token is no longer valid. Please update your connection settings.",
        "data": {
          "host": "Host",
          "port": "Port",
          "token": "Gateway Token",
          "use_ssl": "Use SSL (wss://)"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to Gateway",
      "invalid_auth": "Authentication failed - invalid token",
      "timeout": "Connection timeout",
      "unknown": "Unexpected error occurred"
    },
    "abort": {
      "already_configured": "Gateway is already configured",
      "reauth_successful": "Re-authentication successful"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "OpenClaw Gateway Options",
        "description": "Update Gateway connection settings",
        "data": {
          "host": "Host",
          "port": "Port",
          "token": "Gateway Token",
          "use_ssl": "Use SSL (wss://)",
          "timeout": "Agent Timeout (seconds)",
          "session_key": "Session Key (default: main)",
          "model": "Model override (optional)",
          "thinking": "Thinking mode override (optional)",
          "strip_emojis": "Strip emojis from TTS speech",
          "tts_max_chars": "TTS max characters (0 = no limit)"
        }
      }
    }
  },
  "issues": {
    "gateway_auth_failed": {
      "title": "Gateway authentication failed",
      "description": "The OpenClaw Gateway token is no longer valid. Update your token in Settings > Devices & Services > OpenClaw > Configure."
    }
  }
}
